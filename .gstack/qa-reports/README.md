# .gstack/qa-reports

本目录保存 repo-native QA evidence。

QA 报告应列出实际执行的命令、结果、失败项、修复记录和未覆盖风险。不能只写“应该通过”或“计划验证”；完成声明必须基于真实命令、截图或可复现检查。

如果任务改变 runtime、plugin、adapter 或协作门禁，QA 报告应包含对应的 smoke、schema guard、spec sync 和插件校验结果。
