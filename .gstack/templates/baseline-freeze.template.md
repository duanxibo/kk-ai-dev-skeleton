# 基线冻结模板（历史流程参考）

新任务默认不再使用 `baseline -> handoff -> stack` 流程。本模板仅用于维护旧 baseline 资产或明确批准的原型实验；进入正式实现前，应把采纳结论同步到 `docs/specs/<module>/`。

- 主题:
- 负责人:
- 日期:
- 状态:
  draft / frozen / superseded
- 范围:
- 相关文档:

## 目标

- 这次要冻结的需求或页面目标是什么

## 用户 / 业务背景

- 目标用户是谁
- 要解决什么问题
- 为什么现在要做

## 冻结行为

- 页面或流程入口
- 关键交互
- 关键状态
- 成功 / 失败 / 空态 / 边界态

## 数据 / 字段语义

- 关键字段定义
- 关键口径说明
- 不允许被工程层自行改写的语义

## 原型 / 基线资产

- 原型链接或文件
- 基线页面或脚本路径
- 配图、样例、fixture 来源

## 非目标范围

- 本次不冻结的部分
- 仍需后续探索的问题

## 工程说明

- 工程承接时必须遵守的约束
- 哪些点允许工程自主实现
- 哪些点必须回到产品确认

## 可进入 stack domain 的信号

- 什么情况下可以认为这份基线可以被采纳进 stack domain spec

## 下一步动作

- 仅在明确批准的历史原型维护场景下留在 `archive/baseline/` 作为归档草稿
- 同步采纳项到 `docs/specs/<module>/`
- 触发 domain-spec-readiness
